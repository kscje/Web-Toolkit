# 文本工具箱 — 产品需求文档（PRD）

> 版本：V1.0 | 状态：规划中 | 更新日期：2026-05-14

---

## 目录

1. [产品概述](#1-产品概述)
2. [目标用户画像](#2-目标用户画像)
3. [核心功能详细规划](#3-核心功能详细规划)
   - [3.1 字数统计工具](#31-字数统计工具)
   - [3.2 保存Markdown工具](#32-保存markdown工具)
   - [3.3 用户建议提交](#33-用户建议提交)
4. [产品架构设计](#4-产品架构设计)
5. [用户界面设计规范](#5-用户界面设计规范)
6. [用户交互流程](#6-用户交互流程)
7. [非功能性需求](#7-非功能性需求)
8. [后续迭代规划](#8-后续迭代规划)
9. [验收标准](#9-验收标准)
10. [附录](#10-附录)

---

## 1. 产品概述

### 1.1 产品名称

**文本工具箱**（英文名：TextFlow）

### 1.2 产品定位

一款轻量级、模块化的谷歌浏览器多功能网页辅助插件。以"即选即用、所见即所得"为核心理念，为用户提供高效便捷的网页内容处理工具集合，覆盖文本统计、格式转换、内容提取等高频网页处理场景。

### 1.3 产品愿景

成为用户浏览网页时的"随身工具箱"——无需切换到第三方应用或在线工具，在浏览器内一站式完成网页内容的处理、转换和提取操作。

### 1.4 核心设计原则

| 原则 | 说明 |
|------|------|
| **即用即走** | 每个工具完成操作不超过3步，无复杂配置流程 |
| **模块独立** | 工具间互不依赖，可单独启用/禁用，支持按需扩展 |
| **隐私优先** | 所有数据处理均在本地浏览器端完成，不收集、不上传任何用户数据 |
| **零侵入** | 插件不修改网页原有DOM结构，不影响页面正常渲染和交互 |
| **渐进增强** | 优先覆盖最常用的两个核心工具（字数统计 + Markdown导出），后续按用户反馈和路线图迭代扩展 |

### 1.5 产品边界

- **包含**：基于当前已加载网页内容的文本统计、格式转换、内容提取
- **不包含**：网页翻译（规划V2）、截图标注（规划V2）、内容收藏（规划V3）、自动化脚本执行、密码管理、广告拦截等

---

## 2. 目标用户画像

### 2.1 核心用户群体

| 用户类型 | 典型场景 | 核心需求 |
|----------|----------|----------|
| **内容创作者** | 写作、编辑、翻译 | 快速统计文章字数、将网页内容转为Markdown保存 |
| **学生/研究者** | 资料搜集、论文写作 | 提取网页正文内容用于引用和整理 |
| **开发者/技术写作者** | 文档整理、技术博客撰写 | 将网页技术文档转为Markdown格式 |
| **运营/编辑人员** | 竞品分析、内容审核 | 统计页面文章数据、保存网页结构信息 |

### 2.2 用户痛点

1. 统计网页字数需要全选复制粘贴到第三方工具，操作繁琐
2. 想要保存网页内容为Markdown格式时，找不到便捷的工具
3. 现有类似工具功能臃肿、广告多、隐私无法保障
4. 部分网页有防复制机制，无法正常选中文本

---

## 3. 核心功能详细规划

### 3.1 字数统计工具

#### 3.1.1 功能概述

为用户提供网页内容的字数统计能力，支持两种统计模式：**选中文本统计**与**全页面统计**。用户可以快速了解当前网页或选中内容的字数、字符数、图片数量和视频数量。

#### 3.1.2 统计维度定义

| 统计项 | 定义 | 适用范围 |
|--------|------|----------|
| **字符数（含空格）** | 包含空格、换行符在内的所有可见和不可见字符 | 选中模式 / 全页模式 |
| **字符数（不含空格）** | 去除空格、制表符、换行符后的字符数 | 选中模式 / 全页模式 |
| **中文字数** | 仅统计Unicode范围内的中文字符（含中文标点） | 选中模式 / 全页模式 |
| **英文单词数** | 以空格分隔的英文单词数量 | 选中模式 / 全页模式 |
| **段落数** | 按换行符拆分的非空段落数量 | 全页模式 |
| **图片数量** | 页面内 `<img>` 标签及CSS背景图片的合计数量 | 全页模式 |
| **视频数量** | 页面内 `<video>` 标签及iframe嵌入视频的合计数量 | 全页模式 |
| **链接数量** | 页面内 `<a href>` 标签数量 | 全页模式 |

> **统计优先级说明：** 选中模式下仅统计"字符数（含/不含空格）"、"中文字数"、"英文单词数"四个维度的文本指标；全页模式下额外统计图片、视频、链接、段落等页面结构指标。

#### 3.1.3 交互流程

```
┌──────────────┐    ┌──────────────┐    ┌──────────────────┐    ┌──────────────┐
│ 用户在网页中 │───▶│ 点击浏览器   │───▶│ 在弹窗中点击     │───▶│ 查看统计结果 │
│ 选中文本     │    │ 工具栏插件图标│    │ "字数统计"按钮   │    │ 面板         │
│ （可选）     │    │              │    │                  │    │              │
└──────────────┘    └──────────────┘    └──────────────────┘    └──────────────┘
```

**详细交互步骤：**

1. **触发入口**：用户点击浏览器工具栏中的插件图标，弹出工具面板
2. **模式判断**：
   - 若用户在网页中**已选中文本** → 弹出标记提示"检测到选中文本，将统计选中内容"，进入选中统计模式
   - 若用户**未选中文本** → 默认进入全页统计模式
3. **执行统计**：用户点击"字数统计"按钮，插件执行统计逻辑
4. **加载状态**：按钮变为加载动画（旋转图标 + "正在统计…"文字），统计耗时目标 <500ms
5. **结果展示**：以信息卡片形式展示各维度统计数据
6. **后续操作**：用户可"重新统计"或切换至另一模式

#### 3.1.4 边界情况处理

| 边界场景 | 处理策略 |
|----------|----------|
| 用户未选中任何文本 | 默认切换为全页统计模式，并显示提示"未检测到选中内容，将统计当前页面全部文字" |
| 选中内容为空或仅含空白符 | 各统计项均显示为0，并提示"选中内容为空" |
| 页面包含iframe嵌套内容 | 能力范围内提取同源iframe内的文本内容参与统计；跨域iframe仅统计iframe标签本身，不穿透提取内容 |
| 页面为动态加载（SPA） | 以当前DOM快照为准进行统计，不等待异步加载 |
| 页面包含大量文本（>10万字） | 采用分段统计算法，确保不阻塞主线程，显示进度提示 |
| 页面无图片/视频 | 对应统计项显示为0，不隐藏该统计项 |
| 页面包含隐藏元素中的文本 | 默认不统计`display:none` / `visibility:hidden` 元素内的文本内容 |

#### 3.1.5 模式切换机制

用户可在统计结果页通过模式切换按钮，在"选中统计"与"全页统计"之间自由切换。切换后自动重新计算并更新结果，无需返回网页重新操作。

---

### 3.2 保存Markdown工具

#### 3.2.1 功能概述

将用户选中的网页内容或全页面核心正文，转换为标准Markdown格式，支持一键复制或下载为`.md`文件，方便用户用于笔记、文档编写和内容归档。

#### 3.2.2 格式转换规则

| 原始HTML元素 | 转换为Markdown格式 | 优先级 |
|-------------|-------------------|--------|
| `<h1>` ~ `<h6>` | `#` ~ `######` 标题 | 必须支持 |
| `<p>` | 段落（保留空行分隔） | 必须支持 |
| `<strong>` / `<b>` | `**粗体**` | 必须支持 |
| `<em>` / `<i>` | `*斜体*` | 必须支持 |
| `<a href="...">` | `[链接文本](URL)` | 必须支持 |
| `<img src="..." alt="...">` | `![alt](src)` | 必须支持 |
| `<ul>` / `<ol>` / `<li>` | `- ` 或 `1. ` 列表 | 必须支持 |
| `<table>` / `<tr>` / `<td>` | GFM表格格式 | 必须支持 |
| `<blockquote>` | `> ` 引用 | 建议支持 |
| `<code>` / `<pre>` | `` ` `` 内联代码 / ` ``` ` 代码块 | 必须支持 |
| `<hr>` | `---` 分隔线 | 建议支持 |
| `<del>` / `<s>` | `~~删除线~~` | 可选支持 |

#### 3.2.3 内容过滤策略

插件需具备智能内容过滤能力，自动识别并剥离以下非核心内容：

| 过滤目标 | 识别方式 | 过滤策略 |
|----------|----------|----------|
| **广告区域** | `[class*="ad"]`, `[id*="ad"]`, `[class*="banner"]` 等常见广告命名约定 | 完全移除，不纳入Markdown输出 |
| **导航栏** | `<nav>`, `[role="navigation"]`, 常见class如`navbar`/`nav`/`header` | 完全移除 |
| **侧边栏** | `<aside>`, `[role="complementary"]`, 常见class如`sidebar`/`side` | 完全移除 |
| **评论区** | 常见评论容器class（如`comment`/`disqus`/`discussion`） | 默认移除，用户可选择保留 |
| **页脚** | `<footer>`, 常见class如`footer`/`copyright` | 默认移除 |
| **弹窗/模态框** | `[role="dialog"]`, 常见class如`modal`/`popup`/`overlay` | 完全移除 |
| **社交分享按钮** | 常见class如`share`/`social` | 完全移除 |

> **用户可控开关**：在工具设置中提供"内容过滤"开关，默认开启。用户关闭后可保留全部原始内容。

#### 3.2.4 导出方式

**方式一：复制Markdown文本**

- 点击"复制"按钮，将生成的Markdown文本写入系统剪贴板
- 复制成功后按钮文字短暂变为"已复制 ✓"（持续1.5秒后恢复）
- 失败时弹出提示"复制失败，请重试或选择下载方式"

**方式二：下载 .md 文件**

- 点击"下载"按钮，触发浏览器下载行为
- 文件名规则：`{网页标题}_{日期}_{时间戳}.md`，示例：`文章标题_20260514_153022.md`
- 若网页标题过长（>50字符）则截断并追加省略号
- 文件编码统一为UTF-8

#### 3.2.5 交互流程

```
┌──────────────┐    ┌──────────────┐    ┌─────────────────┐    ┌─────────────────┐
│ 用户浏览网页 │───▶│ 点插件图标   │───▶│ 点"保存Markdown"│───▶│ 预览Markdown    │
│ 或选中内容   │    │ 打开工具面板 │    │ 按钮            │    │ 渲染效果        │
└──────────────┘    └──────────────┘    └─────────────────┘    └────────┬────────┘
                                                                       │
                                                       ┌───────────────┴───────────────┐
                                                       ▼                               ▼
                                               ┌──────────────┐               ┌──────────────┐
                                               │ 复制到剪贴板 │               │ 下载.md文件  │
                                               └──────────────┘               └──────────────┘
```

**详细交互步骤：**

1. **触发入口**：用户点击插件图标打开工具面板
2. **内容选择判断**：
   - 已选中文本 → 仅转换选中区域HTML为Markdown
   - 未选中文本 → 提取当前页面主体内容（`<article>` / `<main>` / 最大文本块）进行转换
3. **执行转换**：点击"保存Markdown"按钮，展示加载状态（"正在转换…"）
4. **预览展示**：转换完成后在弹窗中展示Markdown渲染预览（左侧源码 / 右侧预览，可切换）
5. **导出选择**：用户选择"复制"或"下载"
6. **完成反馈**：导出成功后给予明确的成功提示

#### 3.2.6 边界情况处理

| 边界场景 | 处理策略 |
|----------|----------|
| 选中内容包含复杂嵌套表格 | 尽力转换为GFM表格格式；无法转换的合并单元格表格，保留为HTML table源码 |
| 图片src为相对路径 | 自动补全为绝对URL，确保导出的Markdown中图片链接可访问 |
| 选中内容包含SVG内联元素 | 保留SVG原始代码，包裹在代码块中 |
| 页面正文极短（<50字符） | 仍完成转换，但提示"内容较少，可能已被过滤较多干扰元素" |
| 转换内容超过1MB | 分段处理，下载模式自动拆分（仍输出单个文件）；复制模式提示"内容过长建议下载" |
| 跨域iframe内容 | 不提取跨域iframe内的内容；同域iframe内容纳入转换范围 |
| 代码块包含特殊字符 | 正确转义，确保Markdown源码中的特殊字符不会导致渲染异常 |

### 3.3 用户建议提交

#### 3.3.1 功能概述

在插件弹窗底部提供"向我们建议更多工具"入口，用户可描述自己需要的工具类型和使用场景，一键提交反馈。该功能旨在建立用户驱动的工具扩展反馈通道，为后续迭代路线图的优先级排序提供真实用户数据参考。

#### 3.3.2 交互流程

```
┌──────────────┐    ┌──────────────┐    ┌───────────────┐    ┌──────────────┐
│ 用户打开插件 │───▶│ 滑动至弹窗   │───▶│ 输入工具建议   │───▶│ 点击"提交     │
│ 工具面板     │    │ 底部建议区域  │    │ 及使用场景     │    │ 建议"按钮    │
└──────────────┘    └──────────────┘    └───────────────┘    └──────┬───────┘
                                                                     │
                                                       ┌─────────────┴─────────────┐
                                                       ▼                           ▼
                                               ┌──────────────┐          ┌────────────────┐
                                               │ 提交成功      │          │ 存储至本地      │
                                               │ 展示感谢反馈  │          │ localStorage   │
                                               └──────────────┘          └────────────────┘
```

**详细交互步骤：**

1. **入口位置**：弹窗内容区域底部，固定在工具列表最下方，以温和的视觉方式呈现
2. **触发方式**：用户可通过滚动自然发现，也可由功能性引导文案吸引注意
3. **输入建议**：点击输入框后激活，用户输入文字描述期望的新工具及其使用场景
4. **提交反馈**：点击"提交建议"按钮或按 Enter 键提交
5. **反馈状态**：
   - 提交成功：按钮短暂变为"✓ 感谢反馈"（持续2秒）+ 清空输入框
   - 输入为空：按钮置灰不可点击，提示"请描述您需要的工具"
   - 提交异常：提示"提交失败，请稍后再试"
6. **数据存储**：建议内容与时间戳一同存入 `chrome.storage.local`，key 为 `user_suggestions`

#### 3.3.3 UI规格

| 属性 | 规范 |
|------|------|
| 位置 | 弹窗内容区底部，工具列表和"即将上线"区域下方 |
| 标题文案 | "💡 需要更多工具？" |
| 引导文案 | "描述你需要的工具类型和使用场景，帮助我们变得更好" |
| 输入框 | 多行文本域（textarea），高度固定约60px，自动纵向滚动 |
| 占位符 | `例：我希望能一键提取网页中所有的表格数据…` |
| 按钮文案 | "提交建议" |
| 字数限制 | 最多500字，右下角实时显示剩余字数 |
| 视觉风格 | 与当前主题一致，相对工具卡片视觉权重较低，避免喧宾夺主 |

#### 3.3.4 边界情况处理

| 边界场景 | 处理策略 |
|----------|----------|
| 用户输入纯空白字符 | 提交前自动trim()，若结果为空则拒绝提交 |
| 输入超过500字 | 阻止继续输入，右下角字数计数变为红色警告 |
| 连续快速提交 | 提交成功后按钮冷却2秒，防止重复提交 |
| localStorage空间不足 | 优先保留最近50条建议，自动清理最旧数据 |
| 用户在输入中途关闭弹窗 | 草稿自动保存至localStorage，下次打开恢复 |
| 网络离线状态 | 功能完全独立于网络，离线可用（离线时提交同样有效） |

#### 3.3.5 数据存储结构

```json
{
  "user_suggestions": [
    {
      "id": "sug_001",
      "content": "我希望能一键提取网页中所有的表格数据...",
      "timestamp": "2026-05-14T15:30:22+08:00",
      "page_url": "https://example.com/article",
      "page_title": "示例文章标题",
      "status": "pending"
    }
  ]
}
```

> **隐私说明**：建议数据仅存储在用户本地浏览器中，不上传至任何服务器。后续V2.0考虑通过匿名统计通道收集热门建议（需用户知情同意）。

---

## 4. 产品架构设计

### 4.1 插件分层架构

```
┌─────────────────────────────────────────────────────┐
│                    用户界面层                          │
│  ┌───────────────┐  ┌───────────────┐  ┌──────────┐ │
│  │ Popup弹窗界面 │  │ Options设置页 │  │ 右键菜单  │ │
│  └───────┬───────┘  └───────┬───────┘  └─────┬────┘ │
├──────────┼──────────────────┼──────────────────┼─────┤
│          ▼                  ▼                  ▼      │
│                    工具管理层                           │
│  ┌───────────────────────────────────────────────┐   │
│  │              工具注册中心 (Tool Registry)       │   │
│  │  ┌──────────┐  ┌──────────┐  ┌─────────────┐ │   │
│  │  │字数统计   │  │Markdown  │  │ 工具N(预留)  │ │   │
│  │  │ Tool     │  │ Tool     │  │ Tool        │ │   │
│  │  └──────────┘  └──────────┘  └─────────────┘ │   │
│  └───────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────┤
│                    核心基础层                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐  │
│  │页面内容  │ │存储管理  │ │消息通信  │ │配置管理 │  │
│  │提取引擎  │ │模块      │ │模块      │ │模块    │  │
│  └──────────┘ └──────────┘ └──────────┘ └────────┘  │
├─────────────────────────────────────────────────────┤
│                 浏览器API层                            │
│  Chrome Extension APIs (Storage / Tabs / Runtime)     │
└─────────────────────────────────────────────────────┘
```

### 4.2 核心模块职责

| 模块 | 文件载体 | 职责说明 |
|------|----------|----------|
| **背景服务** | `background.js` | 插件生命周期管理、工具注册中心、跨页面消息路由、右键菜单注入 |
| **弹窗界面** | `popup.html` + `popup.js` | 展示工具列表入口、各工具UI组件容器、用户交互处理 |
| **内容处理** | `content.js` | 页面DOM内容提取（文本/HTML/媒体元素）、选中内容获取、iframe内容处理 |
| **工具模块** | `tools/` 目录 | 每个工具独立封装：包含工具描述元数据、UI渲染函数、核心处理逻辑 |
| **工具注册中心** | `tool-registry.js` | 维护已注册工具列表、工具启用/禁用状态、工具搜索索引 |
| **存储管理** | `storage-manager.js` | 统一封装 `chrome.storage.local` 读写操作，提供偏好配置存取接口 |
| **配置管理** | `config.js` | 用户偏好设置管理（默认导出格式、过滤开关、主题等） |

### 4.3 工具注册机制设计

为实现灵活的工具扩展，设计标准化的工具注册接口。每个工具需满足以下契约：

```
工具注册规范（Tool Interface Contract）

┌──────────────────────────────────────────────┐
│ 工具元数据（Tool Meta）                        │
│ · id : string           唯一标识符            │
│ · name : string         显示名称              │
│ · description : string  功能描述              │
│ · version : string      工具版本号            │
│ · icon : string         图标资源路径           │
│ · category : string     工具分类              │
│ · enabled : boolean     默认启用状态           │
│ · permissions : []      所需额外权限列表        │
└──────────────────────────────────────────────┘
┌──────────────────────────────────────────────┐
│ 工具行为接口（Tool Behavior Interface）        │
│ · onActivate()     : 工具激活时调用            │
│ · onDeactivate()   : 工具停用时调用            │
│ · execute(context) : 执行核心功能              │
│ · renderUI()       : 返回UI组件配置            │
│ · validate()       : 执行前环境校验            │
└──────────────────────────────────────────────┘
```

**注册流程：**

1. 新工具开发完成后，在 `tools/` 目录下创建独立文件夹
2. 工具入口文件调用 `ToolRegistry.register(toolMeta, toolImpl)` 完成注册
3. 工具注册中心校验接口契约完整性后，将工具纳入管理
4. 弹窗界面自动扫描已注册工具列表，动态渲染工具入口

**工具分类体系：**

| 分类 | 说明 | 示例 |
|------|------|------|
| `text` | 文本处理类 | 字数统计、文本替换 |
| `convert` | 格式转换类 | Markdown导出、PDF导出 |
| `extract` | 内容提取类 | 图片提取、链接提取 |
| `utility` | 通用工具类 | 截图、翻译、搜索 |

### 4.4 数据存储方案

使用 `chrome.storage.local` 作为持久化存储方案（配额5MB，远超插件需求）。

| 存储Key | 存储内容 | 数据类型 | 默认值 |
|---------|----------|----------|--------|
| `user_preferences` | 用户偏好设置 | Object | 见下方结构 |
| `tool_settings` | 各工具独立配置 | Object | `{}` |
| `tool_states` | 工具启用/禁用状态 | Object | 全部启用 |
| `usage_stats` | 本地使用统计（匿名） | Object | `{}` |
| `user_suggestions` | 用户提交的工具建议列表 | Array | `[]` |

**用户偏好设置结构：**

```json
{
  "user_preferences": {
    "default_export_format": "markdown",
    "content_filter_enabled": true,
    "display_mode": "card",
    "auto_detect_selection": true,
    "language": "zh_CN"
  }
}
```

> **安全承诺**：以上所有数据仅存储在用户本地浏览器，绝不向任何服务器上传。插件无需注册账号，无需联网权限（除用户主动下载外）。

### 4.5 消息通信规范

插件各组件间通过 `chrome.runtime.sendMessage` 进行通信，定义统一消息格式：

```
消息格式规范：
{
  "action": "string",    // 操作类型（如 "wordCount" / "saveMarkdown"）
  "source": "string",    // 消息来源（"popup" / "content" / "background"）
  "payload": {},         // 携带数据
  "requestId": "string"  // 唯一请求ID，用于追踪和回调匹配
}

响应格式规范：
{
  "success": true/false,
  "data": {},
  "error": "错误描述（仅失败时）",
  "requestId": "string"
}
```

| 消息Action | 发起方 | 接收方 | 说明 |
|------------|--------|--------|------|
| `wordCount` | popup | content → popup | 触发字数统计，content处理后将结果返回 |
| `saveMarkdown` | popup | content → popup | 触发Markdown转换 |
| `getSelection` | popup | content → popup | 获取当前页面选中内容 |
| `getPageInfo` | popup | content → popup | 获取页面基础信息（标题、URL等） |
| `updatePreference` | popup/options | background | 更新用户偏好设置 |
| `registerTool` | tool模块 | background | 注册新工具 |

---

## 5. 用户界面设计规范

### 5.1 插件图标设计

| 尺寸 | 用途 | 设计要求 |
|------|------|----------|
| 16×16 px | 浏览器工具栏图标 | 高辨识度，单色或双色为主 |
| 32×32 px | 工具栏图标（Retina） | 与16px保持视觉一致 |
| 48×48 px | 扩展管理页面图标 | 细节丰富，展示完整Logo |
| 128×128 px | Chrome应用商店展示 | 完整品牌呈现 |

**图标设计方向**：以"工具箱/瑞士军刀"为设计隐喻，采用简洁几何图形组合（扳手+齿轮/方块堆叠），配色方案以深蓝（#1A73E8，Chrome品牌蓝色系）+ 白色为主，确保在各种背景色下均可识别。

### 5.2 弹窗布局设计

弹窗尺寸：**380px × 520px**（固定宽度，高度自适应内容，最大520px）

```
┌─────────────────────────────────┐
│  🔧 文本工具箱          ⚙ ⓘ  │  ← 顶部标题栏
├─────────────────────────────────┤
│  ┌──────────┐  ┌──────────────┐ │
│  │  📊      │  │  📝          │ │
│  │ 字数统计  │  │ 保存Markdown  │ │  ← 工具卡片区
│  │          │  │              │ │     (卡片式布局)
│  └──────────┘  └──────────────┘ │
│  ┌──────────┐                   │
│  │  🔮      │                   │
│  │ 更多工具  │   敬请期待...     │  ← 预留扩展位
│  └──────────┘                   │
├─────────────────────────────────┤
│  统计模式选择     选中 / 全页    │  ← 工具操作区
│  ┌─────────────────────────────┐│    (点击某个工具后展开)
│  │  中文字数:    1,234 字      ││
│  │  英文单词:    567 words     ││
│  │  字符(含空格): 2,345        ││
│  │  字符(无空格): 2,100        ││
│  │  图片数量:    5 张          ││
│  │  视频数量:    2 个          ││
│  └─────────────────────────────┘│
│  [重新统计]                     │
├─────────────────────────────────┤
│  💡 需要更多工具？              │  ← 用户建议区
│  ┌─────────────────────────────┐│    （弹窗底部，视觉权重较轻）
│  │ 描述你需要的工具……          ││
│  └─────────────────────────────┘│
│  [提交建议]                     │
├─────────────────────────────────┤
│  ⚡ 处理耗时：120ms              │  ← 状态栏
└─────────────────────────────────┘
```

### 5.3 界面设计规范细则

| 规范项 | 要求 |
|--------|------|
| **布局方式** | 卡片式布局，默认首页展示所有工具入口卡片（2列网格） |
| **工具详情** | 点击某工具卡片后，在当前弹窗内展开工具详情面板（不跳转页面） |
| **返回导航** | 工具详情页顶部提供"← 返回"按钮，点击回到工具列表首页 |
| **加载状态** | 使用骨架屏（Skeleton Screen）或旋转加载图标，避免空白等待 |
| **结果展示** | 统计数据以"标签+数值"的卡片形式清晰呈现，支持数据高亮 |
| **操作反馈** | 所有按钮操作提供即时视觉反馈（hover变色、active按压效果、loading旋转） |
| **错误提示** | Toast轻提示方式（弹窗底部滑入，2.5秒后自动消失），不打断用户操作 |
| **空状态** | 当无选中内容或统计结果为空时，展示友好的空状态插图和说明文字 |
| **主题** | 优先支持浅色主题；深色模式规划为V2功能 |
| **字体** | 使用系统默认字体栈（-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto），确保跨平台一致性 |
| **动效** | 过渡动画时长200-300ms，使用ease-in-out缓动函数 |

### 5.4 交互反馈规范

| 场景 | 反馈形式 | 时机 |
|------|----------|------|
| 点击工具按钮 | 按钮按下效果 + 加载动画 | 即时 |
| 统计/转换处理中 | 按钮内显示旋转图标 + "处理中…"文字 | 处理开始时 |
| 处理完成 | 数据面板展开 + 按钮恢复默认状态 | 处理完成时 |
| 处理失败 | Toast提示错误原因 + 按钮恢复可点击状态 | 出错时 |
| 复制成功 | 按钮文字短暂变为"已复制 ✓"（1.5秒） | 复制完成后 |
| 下载成功 | Toast提示"文件已开始下载" | 下载触发时 |
| 无内容可处理 | 空状态提示 + 引导操作说明 | 检测到空内容时 |
| 建议提交成功 | 按钮变为"✓ 感谢反馈"（2秒）+ 清空输入框 | 提交完成后 |
| 建议提交失败 | Toast提示"提交失败，请稍后再试" | 出错时 |

---

## 6. 用户交互流程

### 6.1 主流程图

```
                    ┌──────────┐
                    │ 打开网页  │
                    └────┬─────┘
                         │
                         ▼
              ┌─────────────────────┐
              │ 点击插件图标          │
              │ 打开工具面板          │
              └─────────┬───────────┘
                        │
                        ▼
              ┌─────────────────────┐
              │ 展示工具选择首页      │
              │ (卡片式工具列表)      │
              └─────────┬───────────┘
                        │
           ┌────────────┼────────────┐
           ▼            ▼            ▼
   ┌───────────┐ ┌───────────┐ ┌───────────┐
   │ 字数统计   │ │保存Markdown│ │ 更多工具  │
   │ Tool      │ │ Tool      │ │ (预留)    │
   └─────┬─────┘ └─────┬─────┘ └───────────┘
         │             │
         ▼             ▼
   ┌───────────┐ ┌───────────┐
   │ 执行统计   │ │ 执行转换   │
   │ 展示结果   │ │ 预览+导出  │
   └───────────┘ └───────────┘
```

### 6.2 字数统计完整用户旅程

| 步骤 | 用户操作 | 系统响应 | 耗时目标 |
|------|----------|----------|----------|
| 1 | 在网页中选中需要统计的文本 | 无自动响应（不干扰用户） | — |
| 2 | 点击浏览器工具栏插件图标 | 弹窗展开，显示工具列表首页 | <200ms |
| 3 | 点击"字数统计"卡片 | 进入字数统计工具面板，自动检测选中状态并标注当前模式 | <100ms |
| 4 | 点击"开始统计"按钮 | 按钮进入加载状态，执行统计逻辑 | — |
| 5 | （等待） | 统计完成，展开结果面板，展示各维度数据 | <500ms |
| 6 | （可选）切换"全页/选中"模式 | 重新统计并刷新结果 | <500ms |
| 7 | （可选）点击"重新统计" | 重新执行统计 | <500ms |

### 6.3 保存Markdown完整用户旅程

| 步骤 | 用户操作 | 系统响应 | 耗时目标 |
|------|----------|----------|----------|
| 1 | （可选）在网页中选中需要转换的内容 | 无自动响应 | — |
| 2 | 点击浏览器工具栏插件图标 | 弹窗展开，显示工具列表首页 | <200ms |
| 3 | 点击"保存Markdown"卡片 | 进入Markdown工具面板，显示内容来源（选中/全页） | <100ms |
| 4 | 点击"开始转换"按钮 | 执行HTML→Markdown转换，显示加载状态 | <500ms |
| 5 | （等待） | 转换完成，展示Markdown预览面板（源码/渲染双视图） | — |
| 6a | 点击"复制"按钮 | 将Markdown文本写入剪贴板，按钮变为"已复制 ✓" | <100ms |
| 6b | 点击"下载 .md"按钮 | 触发浏览器下载，显示Toast提示 | <200ms |
| 7 | 完成操作 | 可关闭弹窗或继续使用其他工具 | — |

### 6.4 右键菜单快捷操作（规划V1.1）

- 用户在网页中选中文本后，右键菜单出现"统计选中字数"和"保存为Markdown"两个快捷选项
- 点击后直接在当前页面以小弹窗形式展示结果，无需打开插件弹窗
- 减少操作步骤：从3步缩短为2步

---

## 7. 非功能性需求

### 7.1 性能要求

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| 工具处理响应时间 | ≤ 500ms（P95） | 从用户点击到结果展示的完整耗时 |
| 插件弹窗打开速度 | ≤ 200ms | 从点击图标到弹窗完全渲染 |
| 页面加载影响 | 对原网页加载时间影响 < 50ms | content.js注入执行耗时 |
| 内存占用 | 空闲状态 < 30MB，工作状态 < 60MB | Chrome任务管理器 |
| CPU占用 | 处理期间CPU峰值 < 20%（单核） | Chrome Performance Monitor |
| 大页面处理（>1MB文本） | ≤ 2s | 10万字级别页面统计耗时 |

### 7.2 兼容性要求

| 兼容项 | 要求 |
|--------|------|
| **浏览器** | Google Chrome 80 及以上版本 |
| **Chromium内核** | Microsoft Edge、Brave、Opera等主流Chromium浏览器（尽力兼容） |
| **操作系统** | Windows 10+、macOS 10.15+、Linux（主流发行版）、ChromeOS |
| **屏幕分辨率** | 支持 1366×768 及以上分辨率，弹窗在HiDPI/Retina屏幕下清晰显示 |
| **网页类型** | 兼容主流新闻站、博客、文档站、GitHub等代码托管平台、技术社区 |
| **Manifest版本** | 使用 Manifest V3 标准 |

### 7.3 安全性要求

| 安全要求 | 具体措施 |
|----------|----------|
| **权限最小化** | 仅申请必要的 `activeTab`、`storage`、`contextMenus`、`clipboardWrite` 权限 |
| **不收集数据** | 不向任何外部服务器发送用户浏览数据、文本内容、使用行为 |
| **无远程代码执行** | 所有代码打包在插件内，不远程加载或执行外部脚本 |
| **CSP内容安全策略** | 配置严格CSP，禁止inline脚本和外部资源加载 |
| **输入过滤** | 对用户选中内容和页面DOM内容进行安全过滤，防止XSS |
| **敏感信息保护** | 不在localStorage中存储任何密码、token等敏感信息 |
| **更新安全** | 通过Chrome Web Store官方渠道更新，支持自动更新签名验证 |

### 7.4 可扩展性设计

| 扩展点 | 设计策略 |
|--------|----------|
| **新增工具** | 通过工具注册接口，新工具只需实现标准契约即可集成，无需修改核心代码 |
| **工具自定义** | 用户可启用/禁用特定工具，自定义工具在弹窗中的排列顺序 |
| **功能增强** | 现有工具可通过版本号管理进行功能迭代，向下兼容 |
| **第三方工具** | 预留外部工具加载接口（远期规划），支持社区贡献工具模块 |
| **国际化** | UI文案集中管理，预留i18n文本映射表，支持后续多语言 |
| **主题系统** | 预留CSS变量体系，支持后续主题切换扩展 |

### 7.5 可用性要求

| 可用性指标 | 目标值 |
|------------|--------|
| 核心任务完成步骤 | ≤ 3步 |
| 首次使用上手时间 | ≤ 30秒（无需教程即可完成一次统计/导出操作） |
| 连续使用稳定性 | 连续使用10次无崩溃或明显错误 |
| 错误恢复 | 任何错误发生后，用户无需刷新页面即可重新操作 |

---

## 8. 后续迭代规划

### 8.1 版本路线图

```
V1.0 ──────▶ V1.1 ──────▶ V1.2 ──────▶ V2.0 ──────▶ V2.1+
(当前规划)   (体验增强)   (工具扩展)   (平台升级)   (生态建设)
```

### 8.2 各版本规划详情

#### V1.0 — 核心版本（当前规划）

| 功能模块 | 内容 |
|----------|------|
| 字数统计工具 | 选中统计 + 全页统计，字符数/中文字数/英文单词数/图片数/视频数 |
| 保存Markdown工具 | HTML→Markdown转换，复制+下载两种导出方式 |
| 用户建议提交 | 弹窗底部建议入口，用户可描述需要的更多工具类型和使用场景 |
| 基础UI框架 | 卡片式布局、工具注册中心、偏好设置存储、建议收集 |
| 右键菜单 | —（规划至V1.1） |

#### V1.1 — 体验增强版

| 功能模块 | 内容 |
|----------|------|
| 右键菜单集成 | 选中文本后右键快捷操作"字数统计"和"保存Markdown" |
| 快捷键支持 | 可自定义快捷键直接触发指定工具 |
| 统计历史 | 本地记录最近10次统计结果，支持快速回溯比较 |
| 导出格式扩展 | Markdown导出支持自定义文件名模板 |
| 暗色模式 | 跟随系统/手动切换深色主题 |

#### V1.2 — 工具扩展版

| 新增工具 | 功能描述 |
|----------|----------|
| **网页翻译工具** | 选中文本翻译（调用Chrome内置翻译API），支持多语言互译，结果显示在弹窗中 |
| **内容收藏工具** | 一键收藏当前页面或选中内容，支持标签分类和导出收藏列表 |
| **链接提取工具** | 提取当前页面所有链接，支持按域名过滤、去重、导出为CSV/TXT |
| **图片提取工具** | 提取当前页面所有图片URL，支持按尺寸过滤、预览、批量下载 |

#### V2.0 — 平台升级版

| 功能/特性 | 内容 |
|-----------|------|
| **工具市场** | 用户可浏览和安装社区贡献的工具模块 |
| **工具组合** | 支持创建"工作流"：一次触发，多个工具串联执行（如：先字数统计，再自动导出统计报告） |
| **云端配置同步** | 用户偏好和工具配置通过Google账号同步（可选，需用户授权） |
| **用户自定义工具排序** | 拖拽调整工具卡片排列顺序 |
| **高级过滤规则** | Markdown导出支持用户自定义过滤规则（CSS选择器白名单/黑名单） |

#### V2.1+ — 生态建设

| 方向 | 内容 |
|------|------|
| 开发者文档 | 开放工具开发SDK、API文档、示例项目 |
| 社区工具审核 | 建立社区工具提交审核机制 |
| 多浏览器支持 | 适配Firefox Add-on标准，覆盖更多用户 |

### 8.3 功能增强方向（持续迭代）

| 增强方向 | 说明 |
|----------|------|
| **智能识别** | 字数统计工具支持自动识别中/英/日/韩等多语种，分类统计 |
| **批量处理** | 支持同时统计多个已打开标签页的字数 |
| **报告生成** | 基于统计结果自动生成简易分析报告（PDF/HTML格式） |
| **OCR支持** | 对网页中图片内的文字进行OCR识别和统计（远期规划） |
| **协作分享** | 统计结果支持一键生成分享链接（匿名、有时效性） |

---

## 9. 验收标准

### 9.1 功能完整性

| 编号 | 验收项 | 验收标准 | 优先级 |
|------|--------|----------|--------|
| FC-01 | 字数统计-选中模式 | 选中页面任意文本，点击统计按钮后，正确显示字符数（含/不含空格）、中文字数、英文单词数 | P0 |
| FC-02 | 字数统计-全页模式 | 未选中文本时，正确统计当前页面全部内容的文本指标和媒体指标 | P0 |
| FC-03 | 字数统计-模式切换 | 可在选中/全页模式之间自由切换，切换后数据正确刷新 | P0 |
| FC-04 | 字数统计-空状态 | 选中空白内容时，各统计项显示为0并给出友好提示 | P1 |
| FC-05 | Markdown导出-选中模式 | 选中含标题/列表/链接/图片/表格的HTML内容，转换后Markdown格式正确 | P0 |
| FC-06 | Markdown导出-全页模式 | 未选中时提取页面正文，输出符合标准Markdown格式的文本 | P0 |
| FC-07 | Markdown导出-复制 | 点击复制后，Markdown文本成功写入系统剪贴板，粘贴验证格式正确 | P0 |
| FC-08 | Markdown导出-下载 | 点击下载后，成功下载.md文件，文件内容为UTF-8编码的Markdown文本 | P0 |
| FC-09 | 内容过滤 | 常见广告、导航栏、侧边栏、评论区等干扰内容被正确过滤 | P1 |
| FC-10 | 用户偏好存储 | 关闭插件后重新打开，用户偏好设置（如过滤开关）保持不变 | P1 |
| FC-11 | 用户建议提交 | 在建议区输入文本后点击提交，建议成功存储至localStorage，按钮反馈"✓ 感谢反馈" | P1 |

### 9.2 易用性

| 编号 | 验收项 | 验收标准 |
|------|--------|----------|
| UX-01 | 操作步骤 | 用户完成一次字数统计或Markdown导出，核心操作步骤不超过3步 |
| UX-02 | 加载反馈 | 所有操作执行期间，有明确的loading状态展示 |
| UX-03 | 结果可读性 | 统计结果展示清晰，各维度命名直观，无需额外解释 |
| UX-04 | 错误提示 | 出错时用户能明确知道原因和下一步操作建议 |
| UX-05 | 首次体验 | 新用户无需教程，30秒内完成第一次统计操作 |

### 9.3 稳定性

| 编号 | 验收项 | 验收标准 |
|------|--------|----------|
| ST-01 | 连续使用 | 连续使用10次（交替使用两个工具），无崩溃、无白屏、无明显数据错误 |
| ST-02 | 异常恢复 | 在网络异常、页面结构异常情况下，工具给出明确提示而非崩溃 |
| ST-03 | 内存泄漏 | 连续打开/关闭弹窗20次，内存占用无明显增长 |
| ST-04 | 多标签页 | 在多个标签页同时使用工具，互不干扰 |

### 9.4 兼容性

| 编号 | 验收项 | 测试站点类型 | 验收标准 |
|------|--------|-------------|----------|
| CP-01 | 新闻类网站 | 网易新闻、新浪新闻、BBC News等 | 字数统计和Markdown导出功能正常 |
| CP-02 | 博客平台 | 知乎专栏、CSDN、Medium、WordPress博客等 | 内容提取完整，格式转换正确 |
| CP-03 | 文档类网站 | MDN Web Docs、语雀、GitHub README等 | 代码块转换正确，表格格式保留 |
| CP-04 | 技术社区 | GitHub、Stack Overflow、掘金等 | 正文识别准确，过滤干扰元素 |
| CP-05 | 浏览器版本 | Chrome 80 / 90 / 100+ | 在各版本下均可正常安装和使用 |
| CP-06 | 操作系统 | Windows / macOS / Linux | 功能行为和界面展示一致 |

### 9.5 性能

| 编号 | 验收项 | 目标值 | 测试条件 |
|------|--------|--------|----------|
| PF-01 | 处理响应时间 | ≤ 500ms (P95) | 常规文章页面（5000字以内） |
| PF-02 | 弹窗打开速度 | ≤ 200ms | 冷启动（首次点击） |
| PF-03 | 大页面处理 | ≤ 2s | 10万字级别长文页面 |
| PF-04 | 页面加载影响 | < 50ms | 测量content.js注入对页面load事件的影响 |

---

## 10. 附录

### 10.1 名词解释

| 术语 | 说明 |
|------|------|
| **Popup** | 用户点击浏览器工具栏插件图标后弹出的浮层窗口 |
| **Content Script** | 注入到用户浏览网页中运行的脚本，可访问页面DOM |
| **Background Script** | 在插件后台持续运行的Service Worker，负责插件生命周期管理 |
| **Manifest V3** | Chrome插件的最新清单文件版本标准 |
| **GFM** | GitHub Flavored Markdown，GitHub风格的Markdown扩展语法 |
| **Toast** | 轻量级消息提示组件，短暂显示后自动消失 |
| **DOM** | Document Object Model，网页文档对象模型 |

### 10.2 参考资料

- Chrome Extension 官方文档：https://developer.chrome.com/docs/extensions/
- Manifest V3 迁移指南：https://developer.chrome.com/docs/extensions/mv3/intro/
- CommonMark 规范：https://commonmark.org/
- GFM 规范：https://github.github.com/gfm/

### 10.3 文档修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|------|------|----------|--------|
| V1.0 | 2026-05-14 | 初始版本，完成全部PRD章节 | — |

---

> **文档状态：待评审**
>
> 本文档为"文本工具箱"Chrome插件的初始产品需求规划，涵盖了产品定位、核心功能、架构设计、界面规范、非功能性需求及后续迭代路线图。待相关干系人评审确认后，进入详细设计和开发阶段。
